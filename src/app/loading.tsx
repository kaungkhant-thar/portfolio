import React from "react";
import { LoadingScreen } from "./components/ui/loading-screen";

const Loading = () => {
  return (
    <div>
      <LoadingScreen />
    </div>
  );
};

export default Loading;
